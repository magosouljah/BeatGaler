name: Issue 97 regression patch

on:
  push:
    branches:
      - issue-97-regression-root-fix

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          fetch-depth: 2
      - name: Sanity
        run: echo "runner-ok"
