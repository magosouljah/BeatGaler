name: Task 5.5 Close

on:
  push:
    branches: [v0.9.0-test-noche]

permissions:
  contents: write

jobs:
  close:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout target branch
        uses: actions/checkout@v4
        with:
          ref: v0.9.0-test-noche
          fetch-depth: 0

      - name: Close task 5.5 documentation
        run: |
          python3 - <<'PY'
          from pathlib import Path

          roadmap_path = Path('migration/BeatGaler-roadmap-para-trabajar-con-IAs.md')
          roadmap = roadmap_path.read_text()
          old = '### [ ] 5.5 — Separar papelera y restauración'
          new = '### [x] 5.5 — Separar papelera y restauración'
          if roadmap.count(old) != 1:
              raise SystemExit(f'expected exactly one unchecked 5.5, found {roadmap.count(old)}')
          roadmap_path.write_text(roadmap.replace(old, new, 1))

          registro_path = Path('migration/Registro-de-avance.md')
          registro = registro_path.read_text().rstrip()
          if '### Registro — 5.5' in registro:
              raise SystemExit('Registro 5.5 already exists')
          entry = r'''

### Registro — 5.5

```
Tarea: 5.5 — Separar papelera y restauración
Estado: Terminada
Fecha: 2026-09-08

Base

- Rama: v0.9.0-test-noche
- SHA inicial de esta ejecución: 99ec3ad8f27eecad526623a7816971121e33c6ef
- Última tarea verificada: 5.4 — Separar Available Offline

Cambio realizado

- Se creó `src/features/trash/useTrashActions.ts` como owner de borrado individual, borrado masivo y restauración desde Settings.
- `App.tsx` dejó de poseer `deleteInFlightRef`, `deleteBeat`, `handleRemoveBulk`, `recordOfflineTrashIntent` y `removeBeatFromLibrary`; ahora compone el hook y pasa `handleBeatRestored` a `SettingsPanel`.
- Se conservaron literalmente las confirmaciones y avisos de borrado, la liberación del audio activo en borrado individual, los estados runtime `SYNC_DELETE_STARTED`/`SET_TRASH_SYNC_REQUIRED`/errores y los intents de Trash offline.
- Los borrados online mantienen los mismos límites explícitos de commit del índice (`move-to-trash` y `bulk-remove`).
- La restauración online conserva la regla crítica de no publicar un segundo índice: primero siembra snapshots de metadata/biblioteca, actualiza la vista y vuelve a hidratar artwork; offline sigue recargando la biblioteca durable mediante `loadOfflineLibrary`.
- No se alteró el comportamiento previo de recuperación de biblioteca vacía ni los servicios nativos de Trash.
- No se inició 5.6.

Adaptación de pruebas

- `scripts/run-regressions.mjs` ahora lee `useTrashActions.ts` como owner real de las transiciones Trash, en lugar de exigir que vivan físicamente en `App.tsx`.
- Se añadió un guard de ownership que exige que App solo componga el hook, que delete/bulk/restore permanezcan en `useTrashActions`, que los dos commits de índice de borrado sigan presentes y que restore no llame `commitSnapshot`.
- Los guards existentes de Trash, biblioteca vacía, reconciliación offline, Direct index y runtime permanecieron activos; no se relajó ningún contrato para hacer pasar la matriz.

Archivos afectados

- src/App.tsx
- src/features/trash/useTrashActions.ts
- scripts/run-regressions.mjs
- migration/BeatGaler-roadmap-para-trabajar-con-IAs.md
- migration/Registro-de-avance.md
- migration/BeatGaler-agent-state.md

Comprobaciones ejecutadas

- GitHub Actions `Task 5.5 Trash Actions Applier`, run 34246285595 — SUCCESS.
- `node scripts/run-migration-checks-with-logs.mjs` — PASS; el step terminó SUCCESS, lo que exige exit 0 de toda la matriz.
- `npm run test:typecheck` — PASS.
- `npm run test:unit:ts` — PASS.
- `npm run test:component:dom` — PASS.
- `npm run test:integration` — PASS.
- `npm run test:regressions` — PASS.
- `npm run build:web` — PASS.
- `npm run build` — PASS.
- `git diff --check` — PASS antes del commit publicado por el workflow.
- SHA de implementación verificada: c5b9843cd093a008a0adaa8a615327c060a7802c.

Comprobaciones no ejecutadas

- `npm run check` — no necesario; la matriz ejecutó individualmente los checks aplicables del plan.
- E2E completos de import/upload/download — fuera del alcance de esta extracción de ownership Trash.
- Prueba física Desktop Windows/macOS — no disponible en esta ejecución de GitHub Actions. No se considera bloqueo porque los servicios nativos de Trash/restauración no cambiaron y los contratos relevantes están cubiertos por regressions, typecheck e ambos builds.

Prueba manual

- No ejecutada ni inventada.
- Desktop online: eliminar un beat reproduciéndose, eliminar varios beats cloud, restaurar desde Settings y recargar la biblioteca.
- Desktop offline: mover uno y varios beats cloud a Trash, restaurar localmente y después reconectar para reconciliar intents.
- Resultado esperado: mismas confirmaciones/avisos, el audio eliminado se libera, los beats retirados no reaparecen por una recarga indebida y restaurar no crea una segunda publicación de índice.

Pendientes / fuera de alcance

- 5.6 — Separar el renombrado global de tags queda pendiente y no fue iniciado.
- El riesgo previo de `handleRemoveBulk` al operar sobre el snapshot `beats` capturado permanece sin cambios; esta ronda movió ownership sin modificar esa semántica.

Riesgos previos relevantes

- El workflow histórico `probe-task-5.1-productive-temp-auth-compile.yml` ya existía antes de esta ronda y no fue modificado.
- Ningún riesgo nuevo de producto quedó abierto por 5.5.

Herramientas temporales restantes

- Ninguna creada por 5.5 debe permanecer al cerrar. El applier y su script ya fueron eliminados por el commit de implementación; este finalizador se elimina en el commit de cierre.

Fallos encontrados y causa

- Run 34245897420: la primera definición del applier temporal fue inválida; GitHub no creó jobs. Fallo de tooling, sin ejecución de código ni cambios de producto.
- Run 34246236588: el mismo workflow inválido volvió a fallar al versionarse el script temporal; tampoco creó trabajo de producto.
- Run 34246285595: el workflow simplificado ejecutó la extracción y toda la matriz terminó PASS; publicó el commit de implementación y retiró el tooling temporal.

Veredicto

Terminada.

Papelera y restauración quedaron fuera de `App.tsx` conservando borrado individual/masivo, audio, intents offline, runtime, commits explícitos de borrado y la prohibición de doble publicación del índice al restaurar.

Siguiente tarea

5.6 — Separar el renombrado global de tags.

No iniciada.
```
'''
          registro_path.write_text(registro + entry + '\n')

          agent = '''# BeatGaler — Agent State

Fecha de ejecución: 2026-09-08
Rama de trabajo obligatoria: `v0.9.0-test-noche`

## Estado actual

- Tarea trabajada: **5.5 — Separar papelera y restauración**
- Estado: **Terminada**
- Última tarea terminada: **5.5 — Separar papelera y restauración**
- SHA inicial de esta ejecución: `99ec3ad8f27eecad526623a7816971121e33c6ef`
- SHA final de implementación verificada: `c5b9843cd093a008a0adaa8a615327c060a7802c`
- Run de verificación principal: **34246285595 — SUCCESS**
- Comprobaciones pendientes para 5.5: **ninguna**

## Resultado verificado

- `useTrashActions` posee borrado individual, borrado masivo, exclusión de deletes simultáneos y restauración desde Settings.
- Se conservaron confirmaciones/avisos, liberación de audio en borrado individual, runtime e intents offline; los commits online mantienen `move-to-trash` y `bulk-remove`.
- Restore online siembra snapshots antes de renderizar y no llama `commitSnapshot`, evitando una segunda publicación del índice ya realizada por la capa nativa.
- Run 34246285595 terminó PASS en typecheck, unit TS, component DOM, integration, regressions, build:web y build, además de `git diff --check` antes del commit.

## Pendientes concretos / fuera de alcance

- 5.6 — Separar el renombrado global de tags queda pendiente y no fue iniciado.
- El riesgo previo de `handleRemoveBulk` con el snapshot `beats` capturado permanece sin cambios y fuera del alcance de 5.5.
- El workflow histórico `probe-task-5.1-productive-temp-auth-compile.yml` ya existía antes de esta ronda y permanece sin cambios.
- No deben quedar herramientas temporales creadas por 5.5 en el árbol final.

## Siguiente tarea

- **5.6 — Separar el renombrado global de tags**
- Estado: **Pendiente**
- No iniciarla hasta la próxima ronda.
'''
          Path('migration/BeatGaler-agent-state.md').write_text(agent)
          PY

      - name: Commit closure and remove finalizer
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/task-5-5-close.yml
          git add migration/BeatGaler-roadmap-para-trabajar-con-IAs.md migration/Registro-de-avance.md migration/BeatGaler-agent-state.md
          git diff --cached --check
          git commit -m "docs(migration): close task 5.5"
          git push origin HEAD:v0.9.0-test-noche
