name: Issue 97 startup preservation patch

on: push

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: issue-97-temp-auth-artwork-resilience-v2
          fetch-depth: 0
      - name: Patch startup authority failure handling
        run: python scripts/issue97-startup-preserve.py
      - name: Commit bounded patch
        run: |
          rm .github/workflows/issue97-startup-preserve.yml scripts/issue97-startup-preserve.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/App.tsx .github/workflows/issue97-startup-preserve.yml scripts/issue97-startup-preserve.py
          git commit -m "fix(web): keep startup library visible on transient authority failure"
          git push origin HEAD:issue-97-temp-auth-artwork-resilience-v2
