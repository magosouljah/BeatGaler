# PIZARRA

> Contexto temporal y desechable. No es source of truth del estado vivo; GitHub y runtime prevalecen.

## Estado del Blueprint

- `V1.md` → definido.
- `SYSTEM.md` → definido.
- `ROADMAP.md` → definido.
- `BRUNO.md` → depurado a recordatorios útiles.
- `ARCHITECTURE/` → todavía sin arquitecturas canónicas aceptadas.
- Escalón actual → **1. Encontrar y usar**.
- Problema principal actual → **Issue #97 / PR #129: startup + playback**.

## hay un problema en el q los chats de chatgpt no pueden ver logs de los run de actions y no tengo idea porq pasa talvez hay demasiados - activo

## Siguientes 3 pasos del proyecto

### 1. Cerrar startup + playback

Terminar Issue #97 / PR #129 de forma limpia siguiendo `SYSTEM.md`: comportamiento correcto, evidencia suficiente, regresiones revisadas, limpieza e integración.

### 2. Cerrar factual el Escalón 1

Validar de punta a punta:

```text
abrir BeatGaler
→ ver biblioteca
→ buscar / ordenar
→ escuchar
→ descargar
```

Si aparece un gap real, convertir **el más importante** en el siguiente problema y resolverlo antes de abrir más frentes.

### 3. Entrar al Escalón 2

Revisar el flujo completo:

```text
subir / importar
→ Review
→ metadata / artwork
→ organizar
→ editar
→ guardar
```

Encontrar el problema más importante que impida que ese flujo sea simple y confiable, y resolverlo siguiendo `SYSTEM.md`.
