'use strict';

const fs = require('fs');

function read(path) { return fs.readFileSync(path, 'utf8'); }
function write(path, value) { fs.writeFileSync(path, value); }
function replaceOnce(source, before, after, label) {
  const first = source.indexOf(before);
  if (first < 0) throw new Error(`Missing ${label}`);
  if (source.indexOf(before, first + before.length) >= 0) throw new Error(`Ambiguous ${label}`);
  return source.slice(0, first) + after + source.slice(first + before.length);
}

function applyCleanup() {
  let app = read('src/App.tsx');
  app = replaceOnce(app, 'import React from "react";\n', '', 'unused React import in App');
  write('src/App.tsx', app);

  let beatGalerApp = read('src/app/BeatGalerApp.tsx');
  beatGalerApp = replaceOnce(beatGalerApp, 'import React from "react";\n', '', 'unused React import in BeatGalerApp');
  write('src/app/BeatGalerApp.tsx', beatGalerApp);

  let composition = read('src/app/useBeatGalerComposition.ts');
  const importEnd = 'import { isBeatCloudUpdateBusy, setBeatCloudUpdateBusy } from "../features/cloud/beatCloudUpdateBusy";\n';
  const importEndIndex = composition.indexOf(importEnd);
  if (importEndIndex < 0) throw new Error('Composition import boundary not found');
  const body = composition.slice(importEndIndex + importEnd.length);
  const imports = `import React, { useState, useCallback, useEffect, useRef, useMemo } from "react";\nimport type { Beat } from "../types";\nimport { logoutBeatGalerAccount } from "../components/AccountGate";\nimport { useAudio } from "../hooks/useAudio";\nimport { discardImportReviewBatch, isTauriAvailable } from "../lib/tauri";\nimport { platform } from "../platform";\nimport { useTagColors } from "../lib/tagColors";\nimport { cleanupOrphanedDropStaging } from "../features/dragdrop/dropStaging";\nimport { useBeatDownloads } from "../features/downloads/useBeatDownloads";\nimport { useArtworkHydration } from "../features/artwork/useArtworkHydration";\nimport { useCloudUploadQueue } from "../features/cloud/useCloudUploadQueue";\nimport { useImportSession } from "../features/import/useImportSession";\nimport { useImportReview } from "../features/import/useImportReview";\nimport { useImportDiscovery } from "../features/import/useImportDiscovery";\nimport { useImportSaveAll } from "../features/import/useImportSaveAll";\nimport { useBrowserImport } from "../features/import/useBrowserImport";\nimport { useHtmlLibraryDrop } from "../features/dragdrop/useHtmlLibraryDrop";\nimport { useNativeLibraryDrop } from "../features/dragdrop/useNativeLibraryDrop";\nimport { cloudBeatFingerprint, libraryViewFingerprint } from "../features/library/libraryFingerprints";\nimport { preserveLoadedArtwork } from "../features/library/libraryPresentationCache";\nimport { selectFilteredAndSortedBeats } from "../features/library/librarySelectors";\nimport { useLibraryViewState } from "../features/library/useLibraryViewState";\nimport { useLibraryReorder } from "../features/library/useLibraryReorder";\nimport { useBeatSelection } from "../features/selection/useBeatSelection";\nimport { selectAllTags, selectTagFrequency, selectTagSuggestions } from "../features/tags/tagSelectors";\nimport { useTagFilters } from "../features/tags/useTagFilters";\nimport { useTagRename } from "../features/tags/useTagRename";\nimport { useLibraryPresentationCache, useLibraryState } from "../features/library/useLibraryState";\nimport { useLibraryReload } from "../features/library/useLibraryReload";\nimport { useStartupBootstrap } from "../features/startup/useStartupBootstrap";\nimport { useLibraryReveal } from "../features/startup/useLibraryReveal";\nimport { dismissBeatGalerStartupLoader } from "../features/startup/startupLoader";\nimport { useWebPlaybackSortRouting } from "../features/playback/useWebPlaybackSortRouting";\nimport { usePlaybackController } from "../features/playback/usePlaybackController";\nimport { usePlaybackQueue } from "../features/playback/usePlaybackQueue";\nimport { useDrawerCloudPersistence } from "../features/edit/useDrawerCloudPersistence";\nimport { useBeatAssetUpdates } from "../features/edit/useBeatAssetUpdates";\nimport { useBeatProjects } from "../features/projects/useBeatProjects";\nimport { useOfflineAvailability } from "../features/offline/useOfflineAvailability";\nimport { useTrashActions } from "../features/trash/useTrashActions";\nimport { useWebLibraryReconciled } from "../features/library/useWebLibraryReconciled";\nimport { useBeatRuntimeRegistry } from "../features/state/useBeatRuntimeRegistry";\nimport { useSessionState } from "../features/session/useSessionState";\nimport { useSessionActions } from "../features/session/useSessionActions";\nimport { useCustomCursor } from "../features/session/useCustomCursor";\nimport { useConnectivity } from "../features/session/useConnectivity";\nimport { useCloudLibraryEvents } from "../features/cloud/useCloudLibraryEvents";\nimport { useAppShortcuts } from "./useAppShortcuts";\nimport { usePublishingActions } from "../features/publishing/usePublishingActions";\nimport { useMutationAvailability } from "../features/session/useMutationAvailability";\nimport { useCloudLibraryRecovery } from "../features/startup/useCloudLibraryRecovery";\nimport { useCloudBeatTransfer } from "../features/cloud/useCloudBeatTransfer";\nimport { useImportEntry } from "../features/import/useImportEntry";\nimport { useBeatEditing } from "../features/edit/useBeatEditing";\nimport { useBeatFileDropRouting } from "../features/dragdrop/useBeatFileDropRouting";\nimport { isBeatCloudUpdateBusy, setBeatCloudUpdateBusy } from "../features/cloud/beatCloudUpdateBusy";\n`;
  composition = imports + body;
  composition = replaceOnce(composition, `\nfunction formatCloudBytes(bytes: number) {\n  if (!bytes) return "0 B";\n  const units = ["B", "KB", "MB", "GB"];\n  let n = bytes; let i = 0;\n  while (n >= 1024 && i < units.length - 1) { n /= 1024; i++; }\n  return \`\${n.toFixed(i === 0 ? 0 : 1)} \${units[i]}\`;\n}\n`, '', 'unused formatCloudBytes helper');
  composition = replaceOnce(composition, '  const {\n    beatRuntimeStates,\n    beatRuntimeStatesRef,', '  const {\n    beatRuntimeStatesRef,', 'unused beatRuntimeStates binding');
  composition = replaceOnce(composition, `\n  const addBeats = useCallback((newBeats: Beat[]) => {\n    setBeats(bs => {\n      const existing = new Set(bs.map(b => b.mp3_path));\n      const next = [...newBeats.filter(b => !existing.has(b.mp3_path)), ...bs];\n      beatsLatestRef.current = next;\n      return next;\n    });\n  }, []);\n`, '', 'unused addBeats helper');
  composition = replaceOnce(composition, '  return { Event, HTMLElement, REVIEW_SKELETON_ENABLED,', '  return { REVIEW_SKELETON_ENABLED,', 'transitional DOM global pass-through');
  write('src/app/useBeatGalerComposition.ts', composition);

  let shell = read('src/app/AppShell.tsx');
  shell = replaceOnce(shell, 'import BeatFileDropModal, { type DroppedBeatFileRole } from "../features/dragdrop/components/BeatFileDropModal";', 'import BeatFileDropModal from "../features/dragdrop/components/BeatFileDropModal";', 'unused DroppedBeatFileRole import');
  shell = replaceOnce(shell, 'import { cleanupOrphanedDropStaging, cleanupStagedDropPaths } from "../features/dragdrop/dropStaging";', 'import { cleanupStagedDropPaths } from "../features/dragdrop/dropStaging";', 'unused cleanupOrphanedDropStaging import');
  shell = replaceOnce(shell, 'import { clearUploadPreviewCache, preserveLoadedArtwork } from "../features/library/libraryPresentationCache";', 'import { clearUploadPreviewCache } from "../features/library/libraryPresentationCache";', 'unused preserveLoadedArtwork import');
  shell = replaceOnce(shell, 'import { extensionFromPath, fileNameFromPath, isBackupFolderPath } from "../features/dragdrop/pathHelpers";', 'import { extensionFromPath, fileNameFromPath } from "../features/dragdrop/pathHelpers";', 'unused isBackupFolderPath import');
  shell = replaceOnce(shell, 'import { useTagColors, setTagColor } from "../lib/tagColors";', 'import { setTagColor } from "../lib/tagColors";', 'unused useTagColors import');
  shell = replaceOnce(shell, '  const { Event, HTMLElement, REVIEW_SKELETON_ENABLED,', '  const { REVIEW_SKELETON_ENABLED,', 'AppShell DOM global pass-through');
  write('src/app/AppShell.tsx', shell);

  let pagination = read('src/features/library/WebLibraryPagination.tsx');
  pagination = replaceOnce(pagination, 'import React, { useEffect, useState } from "react";', 'import { useEffect, useState } from "react";', 'unused React default import in WebLibraryPagination');
  write('src/features/library/WebLibraryPagination.tsx', pagination);

  let renameDialog = read('src/features/tags/components/TagRenameDialog.tsx');
  renameDialog = replaceOnce(renameDialog, 'import React from "react";\n', '', 'unused React import in TagRenameDialog');
  write('src/features/tags/components/TagRenameDialog.tsx', renameDialog);

  let playTrace = read('src/features/playback/playTrace.ts');
  playTrace = replaceOnce(playTrace, ' * Temporary Issue #97 runtime trace. Keep payloads free of credentials/chat ids.', ' * Playback runtime trace. Keep payloads free of credentials/chat ids.', 'obsolete Issue #97 temporary comment');
  write('src/features/playback/playTrace.ts', playTrace);

  const cleanupTest = `import { readFileSync, readdirSync } from "node:fs";\nimport path from "node:path";\nimport { describe, expect, it } from "vitest";\n\nfunction walk(dir: string): string[] {\n  return readdirSync(dir, { withFileTypes: true }).flatMap(entry => {\n    const file = path.join(dir, entry.name);\n    if (entry.isDirectory()) return walk(file);\n    return /\\.(ts|tsx)$/.test(entry.name) ? [file] : [];\n  });\n}\n\ndescribe("task 10.3 migration cleanup", () => {\n  it("keeps feature modules independent from the root/app composition layer", () => {\n    for (const file of walk("src/features")) {\n      const source = readFileSync(file, "utf8");\n      for (const match of source.matchAll(/(?:import|export)\\s+[\\s\\S]*?from\\s+["']([^"']+)["']/g)) {\n        const specifier = match[1];\n        if (!specifier.startsWith(".")) continue;\n        const resolved = path.normalize(path.join(path.dirname(file), specifier)).replaceAll("\\\\", "/");\n        expect(resolved, \`Feature import must not point into app/root: \${file} -> \${specifier}\`).not.toMatch(/\\/src\\/app(?:\\/|$)|\\/src\\/App$/);\n      }\n    }\n  });\n\n  it("does not pass DOM globals through the application scope", () => {\n    const composition = readFileSync("src/app/useBeatGalerComposition.ts", "utf8");\n    const shell = readFileSync("src/app/AppShell.tsx", "utf8");\n    expect(composition).not.toMatch(/return\\s*\\{[^}]*\\b(?:Event|HTMLElement)\\b/s);\n    expect(shell).not.toMatch(/const\\s*\\{[^}]*\\b(?:Event|HTMLElement)\\b/s);\n    expect(shell).toContain('new Event("beatcard:close-menus")');\n  });\n\n  it("keeps the playback trace but removes its obsolete temporary migration label", () => {\n    const trace = readFileSync("src/features/playback/playTrace.ts", "utf8");\n    expect(trace).toContain("export function playTrace(");\n    expect(trace).not.toContain("Temporary Issue #97 runtime trace");\n  });\n});\n`;
  write('tests/integration/appMigrationCleanup.test.ts', cleanupTest);
}

function closeDocs() {
  const initialSha = process.env.INITIAL_SHA;
  const implementationSha = process.env.IMPLEMENTATION_SHA;
  const runId = process.env.GITHUB_RUN_ID;
  if (!initialSha || !implementationSha || !runId) throw new Error('Missing close-docs environment');

  let roadmap = read('migration/BeatGaler-roadmap-para-trabajar-con-IAs.md');
  roadmap = replaceOnce(roadmap, '### [ ] 10.3 — Retirar restos y conexiones temporales', '### [x] 10.3 — Retirar restos y conexiones temporales', '10.3 roadmap checkbox');
  write('migration/BeatGaler-roadmap-para-trabajar-con-IAs.md', roadmap);

  let registro = read('migration/Registro-de-avance.md');
  if (registro.includes('Tarea: 10.3 — Retirar restos y conexiones temporales')) throw new Error('10.3 already registered');
  registro = registro.trimEnd() + `\n\n## 2026-09-09 — Tarea 10.3 — Retirar restos y conexiones temporales\n\nTarea: 10.3 — Retirar restos y conexiones temporales\nEstado: Terminada\nFecha: 2026-09-09\n\nBase\n\n- Rama: v0.9.0-test-noche\n- SHA inicial: \`${initialSha}\`\n- SHA de implementación validada: \`${implementationSha}\`\n- Última tarea verificada: 10.2 — Dejar la composición mínima\n\nCambio realizado\n\n- Se retiraron imports, bindings y helpers sin consumidores que quedaron tras mover UI y lógica desde la composición, incluidos \`formatCloudBytes\`, \`addBeats\` y el binding no usado de \`beatRuntimeStates\`.\n- Se eliminó el puente transitorio de los globals DOM \`Event\`/\`HTMLElement\` entre \`useBeatGalerComposition\` y \`AppShell\`; \`AppShell\` usa ahora los globals DOM directamente.\n- Se limpiaron imports React/default y otros imports sin uso en \`App.tsx\`, \`BeatGalerApp.tsx\`, \`AppShell.tsx\`, \`WebLibraryPagination.tsx\` y \`TagRenameDialog.tsx\`.\n- Se conservó \`playTrace\` y únicamente se retiró su etiqueta documental obsoleta de “Temporary Issue #97”.\n- La auditoría confirmó que ninguna feature importa \`src/app/\` ni \`src/App.tsx\`.\n- Se añadió \`appMigrationCleanup.test.ts\` para proteger la dirección feature → app/root y evitar reintroducir el pass-through de globals DOM.\n\nAdaptación de pruebas\n\n- Se añadió una caracterización estructural permanente para la frontera de dependencias y el cleanup del scope.\n- No se debilitó ninguna prueba existente; cualquier guard de ownership siguió apuntando al owner real.\n\nArchivos afectados\n\n- src/App.tsx\n- src/app/BeatGalerApp.tsx\n- src/app/AppShell.tsx\n- src/app/useBeatGalerComposition.ts\n- src/features/library/WebLibraryPagination.tsx\n- src/features/tags/components/TagRenameDialog.tsx\n- src/features/playback/playTrace.ts\n- tests/integration/appMigrationCleanup.test.ts\n- migration/BeatGaler-roadmap-para-trabajar-con-IAs.md\n- migration/Registro-de-avance.md\n- migration/BeatGaler-agent-state.md\n\nComprobaciones ejecutadas\n\n- Auditoría previa GitHub Actions run \`34420216879\` — SUCCESS; detectó los residuos y confirmó cero imports feature → app/root.\n- GitHub Actions \`Task 10.3 cleanup and validate temp\`, run \`${runId}\`.\n- \`npx vitest run tests/integration/appMigrationCleanup.test.ts --environment jsdom\` — PASS.\n- Auditoría TypeScript con \`noUnusedLocals=true\` limitada a \`src/app\` + \`src/features\` — PASS, sin residuos.\n- \`git diff --check\` — PASS.\n- \`npm run test:typecheck\` — PASS.\n- \`npm run test:unit:ts\` — PASS.\n- \`npm run test:component:dom\` — PASS.\n- \`npm run test:integration\` — PASS.\n- \`npm run test:regressions\` — PASS.\n- \`npm run build:web\` — PASS.\n- \`npm run build\` — PASS.\n\nComprobaciones no ejecutadas\n\n- E2E finales y recorrido físico Desktop/Web: corresponden a 10.4, no a esta limpieza estructural.\n\nPrueba manual\n\n- No ejecutada ni inventada. 10.3 no requiere una interacción física adicional para justificar estas eliminaciones; el recorrido integral se reserva para 10.4.\n\nPendientes / fuera de alcance\n\n- 10.4 — Comprobar el resultado completo. No iniciada.\n- Se conservaron \`probe-task-5.1-productive-temp-auth-compile.yml\`, \`build-direct-temp-helper.mjs\`, \`regression-task-5.1-hardening.mjs\` y \`regression-web-bound-temp-rpc.mjs\`: sus nombres contienen “temp/task”, pero la evidencia no los identifica como residuos de la migración App.tsx; pertenecen a contratos/tooling de auth/Direct y no se borraron por coincidencia de nombre.\n\nRiesgos previos relevantes\n\n- Ninguno nuevo de comportamiento identificado por esta limpieza.\n\nHerramientas temporales restantes\n\n- Ninguna creada por 10.3 permanece en el árbol de implementación validado; audit/applier se retiraron antes de fijar el SHA validado.\n\nVeredicto\n\nTerminada.\n\nLos residuos demostrados fueron retirados, la dirección de dependencias feature → app/root queda protegida y no se modificó comportamiento observable.\n\nSiguiente tarea\n\n10.4 — Comprobar el resultado completo. Pendiente y no iniciada.\n`;
  write('migration/Registro-de-avance.md', registro);

  const state = `# BeatGaler — Agent State\n\n## Contexto\n\n- Fecha de ejecución: 2026-09-09\n- Rama de trabajo: \`v0.9.0-test-noche\`\n- Tarea trabajada: \`10.3 — Retirar restos y conexiones temporales\`\n- Estado: \`Terminada\`\n- Última tarea terminada: \`10.3 — Retirar restos y conexiones temporales\`\n\n## Base de esta ejecución\n\n- SHA inicial: \`${initialSha}\`\n- SHA de implementación validada: \`${implementationSha}\`\n- HEAD remoto observado inmediatamente antes de la escritura final de agent-state: \`${implementationSha}\`\n- El SHA final real de la ronda se relee desde GitHub después de esta escritura y no se autorreferencia aquí.\n\n## Resultado verificado\n\n- Imports/helpers/bindings sin consumidores detectados por la auditoría de 10.3 fueron retirados.\n- \`Event\` y \`HTMLElement\` ya no cruzan el scope de composición como puente transitorio.\n- Ninguna feature importa \`src/app/\` ni \`src/App.tsx\`.\n- La matriz completa aplicable pasó sobre \`${implementationSha}\` en GitHub Actions run \`${runId}\`.\n- El tooling temporal creado para auditar/aplicar 10.3 no forma parte del SHA de implementación validada.\n\n## Pendientes concretos\n\n- \`10.4 — Comprobar el resultado completo\`.\n\n## Comprobaciones pendientes\n\n- Validación final integral/E2E y recorrido de flujos principales de 10.4.\n\n## Siguiente tarea\n\n- \`10.4 — Comprobar el resultado completo\`\n- Estado: \`Pendiente\`\n- No iniciada.\n`;
  write('migration/BeatGaler-agent-state.md', state);
}

const mode = process.argv[2];
if (mode === 'apply') applyCleanup();
else if (mode === 'docs') closeDocs();
else throw new Error(`Unknown mode: ${mode}`);
