import { readFileSync, writeFileSync } from "node:fs";

const appPath = "src/App.tsx";
let app = readFileSync(appPath, "utf8");

function replaceOnce(source, label, before, after) {
  const first = source.indexOf(before);
  if (first < 0) throw new Error(`Task 3.2 patch failed: missing ${label}`);
  if (source.indexOf(before, first + before.length) >= 0) throw new Error(`Task 3.2 patch failed: duplicate ${label}`);
  return source.slice(0, first) + after + source.slice(first + before.length);
}

const transitionCallsBefore = (app.match(/\btransitionRuntime\s*\(/g) ?? []).length;

app = replaceOnce(
  app,
  "runtime imports",
  'import { createBeatRuntimeState, hydrateBeatRuntimeState, transitionBeatRuntimeState, type BeatRuntimeEvent, type BeatRuntimeState } from "./features/state/beatRuntimeState";',
  'import { createBeatRuntimeState } from "./features/state/beatRuntimeState";\nimport { useBeatRuntimeRegistry } from "./features/state/useBeatRuntimeRegistry";',
);

app = replaceOnce(
  app,
  "runtime registry ownership block",
`  // Definitive per-beat operation model. These states are intentionally session-local:\n  // unfinished upload/download/playback work never resurrects after an app restart.\n  // Stable Offline availability is re-hydrated from native durable BeatMeta instead.\n  const [beatRuntimeStates, setBeatRuntimeStates] = useState<Record<string, BeatRuntimeState>>({});\n  const beatRuntimeStatesRef = useRef<Record<string, BeatRuntimeState>>({});\n  useEffect(() => { beatRuntimeStatesRef.current = beatRuntimeStates; }, [beatRuntimeStates]);\n\n  const transitionRuntime = useCallback((beatId: string, event: BeatRuntimeEvent, beatHint?: Beat) => {\n    setBeatRuntimeStates(current => {\n      const beat = beatHint ?? beatsLatestRef.current.find(item => item.id === beatId);\n      const base = current[beatId] ?? createBeatRuntimeState(beat);\n      try {\n        const nextState = transitionBeatRuntimeState(base, event);\n        if (nextState === base) return current;\n        return { ...current, [beatId]: nextState };\n      } catch (error) {\n        // A rejected transition is a programming bug, not a user-facing failure.\n        // Keep the last valid state so races cannot corrupt the machine.\n        console.warn(\`Beat runtime transition rejected for \${beatId}:\`, event.type, error);\n        return current;\n      }\n    });\n  }, []);\n\n  const forgetRuntimeState = useCallback((beatId: string) => {\n    setBeatRuntimeStates(current => {\n      if (!(beatId in current)) return current;\n      const next = { ...current };\n      delete next[beatId];\n      return next;\n    });\n  }, []);\n\n  const clearReconciledTrashRuntimeStates = useCallback(() => {\n    const visibleIds = new Set(beatsLatestRef.current.map(beat => beat.id));\n    setBeatRuntimeStates(current => {\n      let changed = false;\n      const next = { ...current };\n      for (const [id, runtime] of Object.entries(current)) {\n        if (!visibleIds.has(id) && runtime.trash_sync_required) {\n          delete next[id];\n          changed = true;\n        }\n      }\n      return changed ? next : current;\n    });\n  }, []);\n\n  useEffect(() => {\n    // Library reloads/Telegram refreshes must not overwrite transient machines,\n    // but the durable Offline bit always comes from the native source of truth.\n    setBeatRuntimeStates(current => {\n      const next: Record<string, BeatRuntimeState> = {};\n      let changed = false;\n      for (const beat of beats) {\n        const hydrated = hydrateBeatRuntimeState(current[beat.id], beat);\n        next[beat.id] = hydrated;\n        if (hydrated !== current[beat.id]) changed = true;\n      }\n      // A deleting beat leaves the visible library before its cloud-index commit\n      // finishes. Keep that transient state (and the Offline Trash intent bit)\n      // until the responsible operation explicitly clears it.\n      for (const [id, runtime] of Object.entries(current)) {\n        if (next[id]) continue;\n        if (runtime.sync_state === "deleting" || runtime.trash_sync_required) next[id] = runtime;\n        else changed = true;\n      }\n      return changed ? next : current;\n    });\n  }, [beats]);`,
`  const {\n    beatRuntimeStates,\n    beatRuntimeStatesRef,\n    transitionRuntime,\n    forgetRuntimeState,\n    clearReconciledTrashRuntimeStates,\n  } = useBeatRuntimeRegistry(beats, beatsLatestRef);`,
);

const transitionCallsAfter = (app.match(/\btransitionRuntime\s*\(/g) ?? []).length;
if (transitionCallsAfter !== transitionCallsBefore) {
  throw new Error(`Task 3.2 changed runtime operation call count: ${transitionCallsBefore} -> ${transitionCallsAfter}`);
}
if (app.includes("setBeatRuntimeStates(")) throw new Error("Task 3.2 left runtime-state ownership in App.tsx");
if (app.includes("hydrateBeatRuntimeState(")) throw new Error("Task 3.2 left runtime hydration in App.tsx");
if (app.includes("transitionBeatRuntimeState(")) throw new Error("Task 3.2 left runtime transitions in App.tsx");

writeFileSync(appPath, app);

writeFileSync("src/features/state/useBeatRuntimeRegistry.ts", `import { useCallback, useEffect, useRef, useState, type MutableRefObject } from "react";\nimport type { Beat } from "../../types";\nimport {\n  createBeatRuntimeState,\n  hydrateBeatRuntimeState,\n  transitionBeatRuntimeState,\n  type BeatRuntimeEvent,\n  type BeatRuntimeState,\n} from "./beatRuntimeState";\n\nexport type BeatRuntimeRegistry = {\n  beatRuntimeStates: Record<string, BeatRuntimeState>;\n  beatRuntimeStatesRef: MutableRefObject<Record<string, BeatRuntimeState>>;\n  transitionRuntime: (beatId: string, event: BeatRuntimeEvent, beatHint?: Beat) => void;\n  forgetRuntimeState: (beatId: string) => void;\n  clearReconciledTrashRuntimeStates: () => void;\n};\n\nexport function useBeatRuntimeRegistry(\n  beats: Beat[],\n  beatsLatestRef: MutableRefObject<Beat[]>,\n): BeatRuntimeRegistry {\n  // Definitive per-beat operation model. These states are intentionally session-local:\n  // unfinished upload/download/playback work never resurrects after an app restart.\n  // Stable Offline availability is re-hydrated from native durable BeatMeta instead.\n  const [beatRuntimeStates, setBeatRuntimeStates] = useState<Record<string, BeatRuntimeState>>({});\n  const beatRuntimeStatesRef = useRef<Record<string, BeatRuntimeState>>({});\n  useEffect(() => { beatRuntimeStatesRef.current = beatRuntimeStates; }, [beatRuntimeStates]);\n\n  const transitionRuntime = useCallback((beatId: string, event: BeatRuntimeEvent, beatHint?: Beat) => {\n    setBeatRuntimeStates(current => {\n      const beat = beatHint ?? beatsLatestRef.current.find(item => item.id === beatId);\n      const base = current[beatId] ?? createBeatRuntimeState(beat);\n      try {\n        const nextState = transitionBeatRuntimeState(base, event);\n        if (nextState === base) return current;\n        return { ...current, [beatId]: nextState };\n      } catch (error) {\n        // A rejected transition is a programming bug, not a user-facing failure.\n        // Keep the last valid state so races cannot corrupt the machine.\n        console.warn(\`Beat runtime transition rejected for \${beatId}:\`, event.type, error);\n        return current;\n      }\n    });\n  }, [beatsLatestRef]);\n\n  const forgetRuntimeState = useCallback((beatId: string) => {\n    setBeatRuntimeStates(current => {\n      if (!(beatId in current)) return current;\n      const next = { ...current };\n      delete next[beatId];\n      return next;\n    });\n  }, []);\n\n  const clearReconciledTrashRuntimeStates = useCallback(() => {\n    const visibleIds = new Set(beatsLatestRef.current.map(beat => beat.id));\n    setBeatRuntimeStates(current => {\n      let changed = false;\n      const next = { ...current };\n      for (const [id, runtime] of Object.entries(current)) {\n        if (!visibleIds.has(id) && runtime.trash_sync_required) {\n          delete next[id];\n          changed = true;\n        }\n      }\n      return changed ? next : current;\n    });\n  }, [beatsLatestRef]);\n\n  useEffect(() => {\n    // Library reloads/Telegram refreshes must not overwrite transient machines,\n    // but the durable Offline bit always comes from the native source of truth.\n    setBeatRuntimeStates(current => {\n      const next: Record<string, BeatRuntimeState> = {};\n      let changed = false;\n      for (const beat of beats) {\n        const hydrated = hydrateBeatRuntimeState(current[beat.id], beat);\n        next[beat.id] = hydrated;\n        if (hydrated !== current[beat.id]) changed = true;\n      }\n      // A deleting beat leaves the visible library before its cloud-index commit\n      // finishes. Keep that transient state (and the Offline Trash intent bit)\n      // until the responsible operation explicitly clears it.\n      for (const [id, runtime] of Object.entries(current)) {\n        if (next[id]) continue;\n        if (runtime.sync_state === "deleting" || runtime.trash_sync_required) next[id] = runtime;\n        else changed = true;\n      }\n      return changed ? next : current;\n    });\n  }, [beats]);\n\n  return {\n    beatRuntimeStates,\n    beatRuntimeStatesRef,\n    transitionRuntime,\n    forgetRuntimeState,\n    clearReconciledTrashRuntimeStates,\n  };\n}\n`);

const regressionPath = "scripts/run-regressions.mjs";
let regressions = readFileSync(regressionPath, "utf8");
regressions = replaceOnce(
  regressions,
  "runtime registry regression owner",
  '  const runtimeStateMachine = readFileSync(path.join(root, "src", "features", "state", "beatRuntimeState.ts"), "utf8");',
  '  const runtimeStateMachine = readFileSync(path.join(root, "src", "features", "state", "beatRuntimeState.ts"), "utf8");\n  const runtimeRegistry = readFileSync(path.join(root, "src", "features", "state", "useBeatRuntimeRegistry.ts"), "utf8");',
);
regressions = replaceOnce(
  regressions,
  "runtime state ownership regression",
  '  if (!app.includes("const [beatRuntimeStates, setBeatRuntimeStates]")) fail("Runtime states were moved into persisted Beat metadata; transient work must stay session-local.");',
  '  if (!runtimeRegistry.includes("const [beatRuntimeStates, setBeatRuntimeStates]")) fail("Runtime states were moved out of the session-local runtime registry.");\n  if (!runtimeRegistry.includes("const beatRuntimeStatesRef = useRef") || !runtimeRegistry.includes("hydrateBeatRuntimeState") || !runtimeRegistry.includes(\'runtime.sync_state === "deleting" || runtime.trash_sync_required\')) fail("Runtime registry lost hydration/ref ownership or pending Trash preservation.");\n  if (!app.includes("useBeatRuntimeRegistry(beats, beatsLatestRef)")) fail("App flows are no longer connected to the extracted runtime registry.");\n  if (!app.includes(\'type: "SYNC_DELETE_STARTED"\') || !app.includes(\'type: "SET_TRASH_SYNC_REQUIRED"\') || !app.includes(\'type: "SET_OFFLINE_AVAILABLE"\')) fail("Delete/Trash/Offline flows are no longer wired to the definitive runtime registry.");',
);
writeFileSync(regressionPath, regressions);

writeFileSync("tests/component-dom/beatRuntimeRegistry.test.tsx", `// @vitest-environment jsdom\nimport React, { act } from "react";\nimport { createRoot, type Root } from "react-dom/client";\nimport { afterEach, beforeEach, describe, expect, it } from "vitest";\nimport type { MutableRefObject } from "react";\nimport type { Beat } from "../../src/types";\nimport { useBeatRuntimeRegistry, type BeatRuntimeRegistry } from "../../src/features/state/useBeatRuntimeRegistry";\n\n(globalThis as typeof globalThis & { IS_REACT_ACT_ENVIRONMENT?: boolean }).IS_REACT_ACT_ENVIRONMENT = true;\n\nlet host: HTMLDivElement | null = null;\nlet root: Root | null = null;\nlet latestRegistry: BeatRuntimeRegistry | null = null;\nlet latestBeatsRef: MutableRefObject<Beat[]>;\n\nfunction beat(id: string, extra: Partial<Beat> = {}): Beat {\n  return { id, name: id, tags: [], other_files: [], ...extra } as Beat;\n}\n\nfunction Harness({ beats }: { beats: Beat[] }) {\n  latestBeatsRef.current = beats;\n  latestRegistry = useBeatRuntimeRegistry(beats, latestBeatsRef);\n  return <div>{Object.keys(latestRegistry.beatRuntimeStates).join(",")}</div>;\n}\n\nasync function render(beats: Beat[]) {\n  if (!host) {\n    host = document.createElement("div");\n    document.body.appendChild(host);\n    root = createRoot(host);\n  }\n  await act(async () => {\n    root!.render(<Harness beats={beats} />);\n  });\n}\n\nbeforeEach(() => {\n  latestRegistry = null;\n  latestBeatsRef = { current: [] };\n});\n\nafterEach(async () => {\n  if (root) await act(async () => root!.unmount());\n  root = null;\n  host?.remove();\n  host = null;\n  latestRegistry = null;\n});\n\ndescribe("useBeatRuntimeRegistry", () => {\n  it("hydrates visible beats and keeps the same session-local registry ref", async () => {\n    const item = beat("cloud", { telegram_file_id: "file-1", offline_available: true });\n    await render([item]);\n\n    expect(latestRegistry!.beatRuntimeStates.cloud).toMatchObject({\n      sync_state: "synced",\n      download_state: "idle",\n      playback_state: "idle",\n      offline_available: true,\n    });\n    expect(latestRegistry!.beatRuntimeStatesRef.current.cloud).toBe(latestRegistry!.beatRuntimeStates.cloud);\n  });\n\n  it("preserves a deleting beat after its card disappears until the operation forgets it", async () => {\n    const item = beat("deleting", { telegram_file_id: "file-2" });\n    await render([item]);\n    await act(async () => {\n      latestRegistry!.transitionRuntime(item.id, { type: "SYNC_DELETE_STARTED" }, item);\n    });\n\n    await render([]);\n    expect(latestRegistry!.beatRuntimeStates[item.id]?.sync_state).toBe("deleting");\n\n    await act(async () => {\n      latestRegistry!.forgetRuntimeState(item.id);\n    });\n    expect(latestRegistry!.beatRuntimeStates[item.id]).toBeUndefined();\n  });\n\n  it("preserves Offline Trash intent for a missing card and clears it only after reconciliation", async () => {\n    const item = beat("trash", { telegram_file_id: "file-3" });\n    await render([item]);\n    await act(async () => {\n      latestRegistry!.transitionRuntime(item.id, { type: "SET_TRASH_SYNC_REQUIRED", required: true }, item);\n    });\n\n    await render([]);\n    expect(latestRegistry!.beatRuntimeStates[item.id]?.trash_sync_required).toBe(true);\n\n    await act(async () => {\n      latestRegistry!.clearReconciledTrashRuntimeStates();\n    });\n    expect(latestRegistry!.beatRuntimeStates[item.id]).toBeUndefined();\n  });\n\n  it("keeps sync, download and playback transitions in one registry", async () => {\n    const item = beat("ops");\n    await render([item]);\n\n    await act(async () => { latestRegistry!.transitionRuntime(item.id, { type: "SYNC_QUEUE_UPLOAD" }, item); });\n    await act(async () => { latestRegistry!.transitionRuntime(item.id, { type: "SYNC_UPLOAD_STARTED" }, item); });\n    await act(async () => { latestRegistry!.transitionRuntime(item.id, { type: "DOWNLOAD_STARTED" }, item); });\n    await act(async () => { latestRegistry!.transitionRuntime(item.id, { type: "PLAYBACK_PREPARING" }, item); });\n\n    expect(latestRegistry!.beatRuntimeStates[item.id]).toMatchObject({\n      sync_state: "uploading",\n      download_state: "downloading",\n      playback_state: "playback_preparing",\n    });\n  });\n});\n`);

console.log(`Task 3.2 patch applied. Runtime operation calls preserved: ${transitionCallsAfter}.`);
