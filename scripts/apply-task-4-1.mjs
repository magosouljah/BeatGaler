import fs from 'node:fs';

const appPath = 'src/App.tsx';
let app = fs.readFileSync(appPath, 'utf8');

const importNeedle = 'import { decodeArtworkDataUrl } from "./features/artwork/decodeArtworkDataUrl";\n';
if (!app.includes(importNeedle)) throw new Error('Artwork decode import not found');
app = app.replace(importNeedle, 'import { useArtworkHydration } from "./features/artwork/useArtworkHydration";\n');

const cacheImport = 'import { cacheArtworkThumbnail, readCachedArtworkThumbnail } from "./features/artwork/artworkThumbnailCache";\n';
if (!app.includes(cacheImport)) throw new Error('Artwork cache import not found');
app = app.replace(cacheImport, '');

const refNeedle = '  const artworkLoadPromisesRef = useRef<Map<string, Promise<boolean>>>(new Map());\n';
if (!app.includes(refNeedle)) throw new Error('Artwork promise ref not found');
app = app.replace(refNeedle, '');

const start = app.indexOf('  const ensureArtworkReady = useCallback((beat: Beat, allowNetwork = true): Promise<boolean> => {');
if (start < 0) throw new Error('ensureArtworkReady block start not found');
const endMarker = '\n\n  const waitForCookingReady = useCallback';
const end = app.indexOf(endMarker, start);
if (end < 0) throw new Error('ensureArtworkReady block end not found');
app = app.slice(0, start) + app.slice(end + 2);

const hookAnchor = '  const { search, setSearch, sortBy, setSortBy } = useLibraryViewState();\n';
if (!app.includes(hookAnchor)) throw new Error('Library view hook anchor not found');
const hookWiring = `  const handleArtworkHydratedFromNetwork = useCallback((next: Beat[], beatId: string) => {\n    const hydrated = next.find(item => item.id === beatId);\n    if (hydrated && cloudMetaSnapshotRef.current) {\n      cloudMetaSnapshotRef.current.set(beatId, cloudBeatFingerprint(hydrated));\n    }\n    if (cloudLibrarySnapshotRef.current !== null) {\n      cloudLibrarySnapshotRef.current = next\n        .filter(item => !!item.telegram_file_id)\n        .map(cloudBeatFingerprint)\n        .join("\\u001c");\n    }\n  }, []);\n  const { ensureArtworkReady, clearArtworkHydration, invalidateArtworkHydration } = useArtworkHydration({\n    setBeats,\n    onNetworkHydrated: handleArtworkHydratedFromNetwork,\n  });\n`;
app = app.replace(hookAnchor, hookWiring + hookAnchor);

const clearCount = (app.match(/artworkLoadPromisesRef\.current\.clear\(\);/g) || []).length;
if (clearCount < 1) throw new Error('Expected at least one artwork hydration clear');
app = app.replace(/artworkLoadPromisesRef\.current\.clear\(\);/g, 'clearArtworkHydration();');

const invalidateCount = (app.match(/artworkLoadPromisesRef\.current\.delete\(beat\.id\);/g) || []).length;
if (invalidateCount !== 1) throw new Error(`Expected one per-beat artwork invalidation, found ${invalidateCount}`);
app = app.replace(/artworkLoadPromisesRef\.current\.delete\(beat\.id\);/g, 'invalidateArtworkHydration(beat.id);');

if (app.includes('artworkLoadPromisesRef')) throw new Error('Artwork promise ownership remains in App');
if (app.includes('decodeArtworkDataUrl(')) throw new Error('Artwork decode logic remains in App');
if (app.includes('readCachedArtworkThumbnail(')) throw new Error('Artwork cache read remains in App');

fs.writeFileSync(appPath, app);

fs.writeFileSync('src/features/artwork/useArtworkHydration.ts', `import { useCallback, useRef, type Dispatch, type SetStateAction } from "react";\nimport type { Beat } from "../../types";\nimport { platform } from "../../platform";\nimport { cacheArtworkThumbnail, readCachedArtworkThumbnail } from "./artworkThumbnailCache";\nimport { decodeArtworkDataUrl } from "./decodeArtworkDataUrl";\n\ntype ArtworkHydrationOptions = {\n  setBeats: Dispatch<SetStateAction<Beat[]>>;\n  onNetworkHydrated?: (next: Beat[], beatId: string) => void;\n};\n\nexport type ArtworkHydration = {\n  ensureArtworkReady: (beat: Beat, allowNetwork?: boolean) => Promise<boolean>;\n  clearArtworkHydration: () => void;\n  invalidateArtworkHydration: (beatId: string) => void;\n};\n\nexport function useArtworkHydration({ setBeats, onNetworkHydrated }: ArtworkHydrationOptions): ArtworkHydration {\n  const loadPromisesRef = useRef<Map<string, Promise<boolean>>>(new Map());\n\n  const clearArtworkHydration = useCallback(() => {\n    loadPromisesRef.current.clear();\n  }, []);\n\n  const invalidateArtworkHydration = useCallback((beatId: string) => {\n    loadPromisesRef.current.delete(beatId);\n    loadPromisesRef.current.delete(\`\${beatId}:cache\`);\n  }, []);\n\n  const ensureArtworkReady = useCallback((beat: Beat, allowNetwork = true): Promise<boolean> => {\n    const promiseKey = allowNetwork ? beat.id : \`\${beat.id}:cache\`;\n    const existingPromise = loadPromisesRef.current.get(promiseKey);\n    if (existingPromise) return existingPromise;\n\n    const promise = (async () => {\n      const existing = beat.image_preview_base64 || beat.image_base64;\n      if (existing) {\n        const decoded = await decodeArtworkDataUrl(existing);\n        if (decoded) void cacheArtworkThumbnail(beat, existing).catch(() => {});\n        return decoded;\n      }\n\n      const cachedArtwork = await readCachedArtworkThumbnail(beat).catch(() => null);\n      if (cachedArtwork) {\n        const decoded = await decodeArtworkDataUrl(cachedArtwork);\n        if (decoded) {\n          setBeats(current => current.map(item => item.id === beat.id\n            ? { ...item, image_base64: cachedArtwork, image_preview_base64: null }\n            : item));\n          return true;\n        }\n      }\n\n      const hasArtworkReference = Boolean(beat.assets?.artwork?.object_id);\n      if (!allowNetwork) return !hasArtworkReference;\n\n      try {\n        const artwork = await platform.media.loadArtwork(beat);\n        if (!artwork) return true;\n        const presentationArtwork = await cacheArtworkThumbnail(beat, artwork).catch(() => artwork);\n        const decoded = await decodeArtworkDataUrl(presentationArtwork);\n        if (!decoded) return false;\n        setBeats(current => {\n          const next = current.map(item => item.id === beat.id\n            ? { ...item, image_base64: presentationArtwork, image_preview_base64: null }\n            : item);\n          onNetworkHydrated?.(next, beat.id);\n          return next;\n        });\n        return true;\n      } catch (error) {\n        console.warn(\`Artwork warm failed for \${beat.name}:\`, error);\n        return false;\n      }\n    })();\n\n    loadPromisesRef.current.set(promiseKey, promise);\n    void promise.then(ok => {\n      if (!ok) loadPromisesRef.current.delete(promiseKey);\n    });\n    return promise;\n  }, [onNetworkHydrated, setBeats]);\n\n  return { ensureArtworkReady, clearArtworkHydration, invalidateArtworkHydration };\n}\n`);

fs.writeFileSync('tests/component-dom/artworkHydration.test.tsx', `// @vitest-environment jsdom\nimport React, { act, useState } from "react";\nimport { createRoot, type Root } from "react-dom/client";\nimport { afterEach, beforeEach, describe, expect, it, vi } from "vitest";\nimport type { Beat } from "../../src/types";\n\nconst loadArtwork = vi.fn();\nconst cacheArtworkThumbnail = vi.fn();\nconst readCachedArtworkThumbnail = vi.fn();\nconst decodeArtworkDataUrl = vi.fn();\n\nvi.mock("../../src/platform", () => ({ platform: { media: { loadArtwork } } }));\nvi.mock("../../src/features/artwork/artworkThumbnailCache", () => ({ cacheArtworkThumbnail, readCachedArtworkThumbnail }));\nvi.mock("../../src/features/artwork/decodeArtworkDataUrl", () => ({ decodeArtworkDataUrl }));\n\nimport { useArtworkHydration, type ArtworkHydration } from "../../src/features/artwork/useArtworkHydration";\n\n(globalThis as typeof globalThis & { IS_REACT_ACT_ENVIRONMENT?: boolean }).IS_REACT_ACT_ENVIRONMENT = true;\nlet host: HTMLDivElement | null = null;\nlet root: Root | null = null;\nlet api: ArtworkHydration | null = null;\nlet beatsSnapshot: Beat[] = [];\nconst networkHydrated = vi.fn();\n\nconst beat = (overrides: Partial<Beat> = {}): Beat => ({ id: "a", name: "A", bpm: 120, key: "c", tags: [], rating: 0, ...overrides } as Beat);\n\nfunction Harness() {\n  const [beats, setBeats] = useState<Beat[]>([beat()]);\n  beatsSnapshot = beats;\n  api = useArtworkHydration({ setBeats, onNetworkHydrated: networkHydrated });\n  return <div />;\n}\n\nbeforeEach(async () => {\n  loadArtwork.mockReset(); cacheArtworkThumbnail.mockReset(); readCachedArtworkThumbnail.mockReset(); decodeArtworkDataUrl.mockReset(); networkHydrated.mockReset();\n  cacheArtworkThumbnail.mockImplementation(async (_beat: Beat, data: string) => data);\n  decodeArtworkDataUrl.mockResolvedValue(true);\n  readCachedArtworkThumbnail.mockResolvedValue(null);\n  host = document.createElement("div"); document.body.appendChild(host); root = createRoot(host);\n  await act(async () => { root!.render(<Harness />); });\n});\n\nafterEach(async () => { if (root) await act(async () => root!.unmount()); root = null; host?.remove(); host = null; api = null; beatsSnapshot = []; });\n\ndescribe("useArtworkHydration", () => {\n  it("hydrates from presentation cache without network or cloud-snapshot callback", async () => {\n    readCachedArtworkThumbnail.mockResolvedValue("data:cached");\n    let ready = false;\n    await act(async () => { ready = await api!.ensureArtworkReady(beat()); });\n    expect(ready).toBe(true);\n    expect(loadArtwork).not.toHaveBeenCalled();\n    expect(beatsSnapshot[0].image_base64).toBe("data:cached");\n    expect(networkHydrated).not.toHaveBeenCalled();\n  });\n\n  it("deduplicates network hydration and reports only the presentation update", async () => {\n    loadArtwork.mockResolvedValue("data:network");\n    const target = beat({ assets: { artwork: { object_id: "art-1" } } as Beat["assets"] });\n    let first!: Promise<boolean>; let second!: Promise<boolean>;\n    await act(async () => { first = api!.ensureArtworkReady(target); second = api!.ensureArtworkReady(target); await Promise.all([first, second]); });\n    expect(first).toBe(second);\n    expect(loadArtwork).toHaveBeenCalledTimes(1);\n    expect(beatsSnapshot[0].image_base64).toBe("data:network");\n    expect(networkHydrated).toHaveBeenCalledTimes(1);\n  });\n\n  it("does not fetch when cache-only hydration still has a remote artwork reference", async () => {\n    const target = beat({ assets: { artwork: { object_id: "art-1" } } as Beat["assets"] });\n    let ready = true;\n    await act(async () => { ready = await api!.ensureArtworkReady(target, false); });\n    expect(ready).toBe(false);\n    expect(loadArtwork).not.toHaveBeenCalled();\n    expect(networkHydrated).not.toHaveBeenCalled();\n  });\n\n  it("invalidates both network and cache-only memoization for one beat", async () => {\n    loadArtwork.mockResolvedValue("data:first");\n    const target = beat({ assets: { artwork: { object_id: "art-1" } } as Beat["assets"] });\n    await act(async () => { await api!.ensureArtworkReady(target); });\n    expect(loadArtwork).toHaveBeenCalledTimes(1);\n    api!.invalidateArtworkHydration(target.id);\n    loadArtwork.mockResolvedValue("data:second");\n    await act(async () => { await api!.ensureArtworkReady(target); });\n    expect(loadArtwork).toHaveBeenCalledTimes(2);\n  });\n});\n`);

fs.writeFileSync('tests/integration/artworkHydrationExtraction.test.ts', `import { describe, expect, it } from "vitest";\nimport fs from "node:fs";\n\ndescribe("artwork hydration extraction", () => {\n  it("keeps hydration ownership outside App and does not turn display loads into cloud commits", () => {\n    const app = fs.readFileSync("src/App.tsx", "utf8");\n    const hook = fs.readFileSync("src/features/artwork/useArtworkHydration.ts", "utf8");\n    expect(app).toContain('useArtworkHydration({');\n    expect(app).not.toContain('artworkLoadPromisesRef');\n    expect(app).not.toContain('const ensureArtworkReady = useCallback');\n    expect(hook).toContain('platform.media.loadArtwork(beat)');\n    expect(hook).toContain('readCachedArtworkThumbnail(beat)');\n    expect(hook).toContain('cacheArtworkThumbnail(beat, artwork)');\n    expect(hook).toContain('invalidateArtworkHydration');\n    expect(hook).not.toContain('syncBeatMetadataToTelegram');\n    expect(hook).not.toContain('saveBeatMeta');\n    expect(hook).not.toContain('commitSnapshot');\n  });\n});\n`);

console.log('Task 4.1 files prepared');
